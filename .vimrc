syntax on "语法高亮
set autoindent "自动缩进
set smartindent "智能缩进
set expandtab "把输入的tab自动转换成空格，防止不同操作系统tab空格数不一致，python必用～
set tabstop=4 "一个tab键占4个空格
set number " 显示行号
set ruler " 打开状态栏标尺
set shiftwidth=4 " 设定 << 和 >> 命令移动时的宽度为 4
